{# {% extends 'base.html.twig' %}

{% block title %}Valider ma commande - La boutique françaose{% endblock %}

{% block content %}
    <h2>Je passe ma commande</h2>
    <p>Choississez vos préférences avant de passer votre commande sur la boutique française</p>
    <hr>
    <div class="row">
        <div class="col-md-6">
            {% set formhtml %}
                {{ form_start(form, {action:path('app_order_recap')}) }}
                    {{ form_label(form.addresses, 'Choisissez votre adresse de livraison') }}
                    <a href="{{ path('app_account_address_form') }}">Ajouter une nouvelle adresse</a>
                {{ form_end(form) }}
            {% endset %}

            {{ formhtml|replace({'[br]' : '<br>'})|raw }}
        </div>
        <div class="col-md-6">
            <div class="text-center">
                <b>Récap de ma commande</b><br>
                <p>Retrouvez le récapitualif de vos produits</p>
            </div>
            <div class="order-summary">
            {% for key,product in cart %}
                <div class="row {% if key > 0 %}mt-2{% endif %}">
                    <div class="col-2"><img src="/uploads/{{ product.product.illustration }}" alt="{{ product.product.name }}" height="75"></div>
                    <div class="col-8 my-auto">
                        {{ product.product.name }}<br>
                        <small>{{ product.product.subtitle }}</small>
                    </div>
                    <div class="col-2 my-auto">
                        x {{ product.quantity }}
                    </div>
                </div>
            {% endfor %}
            </div>
        </div>
    </div>
{% endblock %} #}

{% extends 'base.html.twig' %}

{% block title %}Mon Panier - La boutique française{% endblock %}

{% block content %}
    <div class="container my-5 small-container">
        <h1>Ma commande</h1>
            {{ form_start(deliverForm) }}
            {{ form_row(deliverForm.addresses) }}
            <a href="{{ path("app_account_address_form") }}" class="small">Ajouter une nouvelle adresse de livraison</a>

            <hr>

            {{ form_row(deliverForm.carriers) }}
            {{ form_end(deliverForm) }}
    </div>
  
{% endblock %}
